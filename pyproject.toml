[project]
name = "pyg-macos-mps"
version = "0.1.0"
description = "macOS/MPS compatibility probes and install workflow for PyTorch Geometric"
requires-python = ">=3.12,<3.13"
dependencies = []

[tool.uv]
package = false
